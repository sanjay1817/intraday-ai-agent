"""AI Intraday Trading Agent application package.

This package root exposes no public symbols; submodules are imported
explicitly by the application factory (`app.main`) to keep import graphs
explicit and avoid circular-import surprises across layers.
"""
