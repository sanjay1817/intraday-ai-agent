"""SQLAlchemy 2.x ORM models.

Every model module must be imported here so that `Base.metadata` is fully
populated before Alembic autogeneration or `create_all` runs. Populated in
Section 8 once the first models exist.
"""
