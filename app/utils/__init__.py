"""Small, stateless, framework-agnostic helper functions shared across
layers (e.g. datetime helpers, string formatting). Must not import from
`app.core`, `app.database`, or any FastAPI-aware module.
"""
