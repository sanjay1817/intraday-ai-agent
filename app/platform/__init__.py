"""Enterprise Quality & Reliability platform primitives.

Cross-cutting infrastructure — service registration, startup/shutdown
lifecycle, health aggregation, feature flags, configuration validation,
plugin discovery, and cache management — that every other module can be
wired into without any of them needing to know about each other. No
business logic lives here.
"""
