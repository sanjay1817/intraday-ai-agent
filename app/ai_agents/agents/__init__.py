"""The nine specialized agents.

Every module here defines a `BaseAgent` subclass decorated with
`@register_agent` (see `app.ai_agents.agent_registry`) — none are
imported directly by the orchestrator.
"""
